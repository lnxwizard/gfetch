package ascii

// Final Result
/*
			.--.
	       |o_o |
	       |:_/ |
	      //   \ \
	     (|     | )
	    /'\_   _/'\
		\___)=(___/
*/

func TuxASCII() error {
	/*fetch := []string{
		"     .--.			%s@%s",
		"    |o_o | 		%s",
		"    |:_/ | 		%s",
		"   //   \\ \\		%s",
		"  (|     | )	",
		" /'\\_   _/`\\	",
		" \\___)=(___/	",
	}*/

	/*currentUser, err := user.GetUsername()
	if err != nil {
		return err
	}

	// hostname
	hostname, err := user.GetHostname()
	if err != nil {
		return err
	}

	disInfo, err := system.GetDistro()
	if err != nil {
		return err
	}

	memInfo, err := system.GetMemoryInfo()
	if err != nil {
		return err
	}*/
	/*
		fmt.Printf("     .--.		%s@%s\n", currentUser, hostname)
		fmt.Printf("    |o_o | 		%s\n", strings.Repeat("-", len(currentUser+hostname)+1))
		fmt.Printf("    |:_/ | 		Distro: %s\n", disInfo)
		fmt.Printf("   //   \\ \\	  	Memory: %s\n", memInfo)
		fmt.Printf("  (|     | )		%s\n")
		fmt.Printf(" /'\\_   _/`\\	%d\n")
		fmt.Printf(" \\___)=(___/	%t\n")*/

	return nil
}

/*
	fmt.Printf("%s@%s\n", currentUser, hostname)
	fmt.Printf("%s\n", disInfo)
	fmt.Printf("%s\n", memInfo)
*/
